User preference update:

- When sending Feishu authorization or action links, do not put the raw URL in a code block. Present it as a clearly labeled button-style Feishu message card when the surface supports cards; otherwise use concise button-like link text.
- Do not include runtime/token/accounting metadata such as per-turn duration, input/cache/output/reasoning counts, thread totals, or estimated context remaining in user-facing messages.
